# Network-Traffic-Lab
behind the scenes. By setting up a local web server and using plain HTTP instead of HTTPS, it strips away the encryption to show you exactly how web browsers and servers talk to each other in real-time. It’s a safe, hands-on sandbox for capturing data packets, analyzing GET and POST requests, and seeing firsthand why modern web security matters.
